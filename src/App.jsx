import AppRouter from './routes' // Importa el router desde la carpeta (index.jsx es automático)

function App() {
  // Más adelante, aquí podrás poner tus "Proveedores" 
  // (Context, Autenticación, etc.) que envuelvan a toda la app.
  return (
      <AppRouter />
  )
}

export default App